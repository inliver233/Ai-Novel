# Ai-Novel 第一阶段实现、验证与后续整改交接文档

> **文档定位**：这是下一轮对话/下一位维护者接手 `Ai-Novel` 118 项长期整改任务时的首要现状来源。
>
> **实现快照**：`dev` / `origin/dev` 在 `508399b`（2026-07-12）时的状态。本文档本身会在该实现快照之后单独提交，因此阅读实现历史时应以 `df31091..508399b` 为第一阶段代码范围。
>
> **最重要的结论**：第一阶段完成了大量关键止血、事务、并发、RAG、认证、迁移、CI 和前端错误契约工作，但 **118 个 canonical audit item 绝没有全部闭环**。76 个 commit 也不等于完成 76 个问题；其中很多是同一问题的子节点、真实 PostgreSQL/Linux CI 暴露后的测试修正或诊断提交。

---

## 1. 新接手者先读什么

建议按以下顺序建立上下文，不要只读本文后直接改代码：

1. `详细任务指引.txt`：长期任务的流程、Git 纪律、终审裁决和 118 项口径。
2. 本文：第一阶段实际做了什么、证据是什么、剩余什么。
3. `.workflow/.scratchpad/remediation_progress.md`：逐节点的本地执行台账和更细测试记录；该目录受保护且默认不提交。
4. `.workflow/.scratchpad/catalog_118_reconciliation.csv`：118 项的原始规范主键、基线状态、严重度、证据和缺口。
5. `朋友简单的review意见.txt`：外部复审指出的优先级纠偏和剩余风险。
6. `项目情况完全分析.md` 与 `.workflow/.scratchpad/full-analysis/`：原始全量审计依据。
7. `.github/workflows/ci.yml`、`backend/scripts/run_quality_gate.py`、前后端 tests README：理解门禁口径。

规范问题主键始终使用 `domain#ordinal`，例如 `backend-api#8`。不要把 M/H/C 历史编号当作主键。

---

## 2. 当前仓库和证据快照

### 2.1 Git 状态

- 工作目录：`E:\F-Ai-Novel\Ai-Novel`
- 开发分支：`dev`
- 上游：`origin/dev`
- 第一阶段已核对实现 HEAD：`508399b fix(characters): surface stale load failures`
- 第一阶段起始基线：`df31091`
- `df31091..508399b`：**76 commits**
- 累计改动：**310 files changed, +27,985 / -7,622**
- 提交类型：43 `fix` / 13 `refactor` / 13 `test` / 4 `ci` / 3 `feat`
- 实现快照时 tracked 工作区干净；仅保留受保护的 untracked 审计资料。

### 2.2 最新 GitHub CI

`508399b` 对应 GitHub Actions run `29181366394`，最终为 `completed / success`，四个 job 全绿：

1. `Frontend (vitest + tsc + eslint)`
2. `Auth rate limit (real Redis)`
3. `PostgreSQL + pgvector schema contract`
4. `Backend (pytest + ruff)`

前序 `f5ca3ae`、`ef15da7`、`aa7b64d` 等最终提交也均四 job 全绿。**但 Frontend job 中全量 `npm run lint` 明确配置为 `continue-on-error: true`；真正的硬门禁是 tests 目录 ESLint/Prettier、测试类型检查、Vitest 和 build。** 因此 job 绿色不代表业务源码全量 lint 绿色。

### 2.3 历史红 CI 应怎样理解

第一阶段有多次“3 个成功、1 个失败”。不能只看主修复 commit 的红灯就判断当前仍坏，也不能反过来把后续绿灯解释为主修复一开始就没有问题。

典型链路：

- `f384ed9` 章节事务修复后，真 PG fixture 暴露 active outline FK 和排序 ID 问题；经 `d6c023a`、`158fdb8` 后最终全绿。
- `a868f76` KB 隔离后，Linux/PG legacy fixture 缺 timestamp/admin 字段；经 `8633d09`、`7ac4499`、`a0e1082` 后最终全绿。
- `ff0de6e` worker 独占 claim 后，SQLite/Linux fixture 建表和 ID 隔离不完整；经 `e294639`、`2afd66e`、`957bd7c` 后最终全绿。
- `41c6b89` / `01c0f3e` 的维度、中文检索改动先后暴露 trigram 执行计划隔离问题；`0ca10cd` 后最终全绿。

交接时应以每条修复链的**最终后继 commit**和当前 HEAD 的绿 CI 为准，同时保留历史红灯作为“真实环境曾发现什么”的证据。

关键最终 run 索引（更细的定向测试数见本地 `remediation_progress.md`）：

| 最终 commit / 链 | GitHub run | 主要证据 |
|---|---:|---|
| `6e5ca23` | `29127724638` | schema contract + 真 PostgreSQL/pgvector |
| `462aa9f` | `29128754684` | generation route/service 分层 |
| `e218f62` | `29129856988` | vector backend/facade 统一 |
| `f442392` | `29131118091` | bootstrap 集中 |
| `41b8e7e` | `29132493331` | config fail-fast |
| `955d3ec` | `29133291954` | API helper/service 分层 |
| `320dfb4` 链最终 `66696d6` | `29137467179` | 真 Redis 限流 + 四 job |
| `afb77e1` 链最终 `11b36a1` | `29139042709` | GET 无副作用 + 真 PG sync 并发 |
| `a727201` 链最终 `78efcfc` | `29141559887` | batch quota 真 PG 并发/EXPLAIN |
| `b011f11` | `29144154058` | batch command/state 重构 |
| `f384ed9` 链最终 `158fdb8` | `29145639490` | 章节 replace 真 PG 原子性 |
| `a868f76` 链最终 `a0e1082` | `29147448169` | KB 隔离、legacy migration/fixture |
| `ff0de6e` 链最终 `957bd7c` | `29149247685` | worker 独占 claim |
| `2d25b00` | `29150789013` | migrator 成功/失败真实 PG 路径 |
| `e45b441` | `29154093455` | disabled session revocation |
| `f615f12` | `29155988057` | generation 显式 prompt sync |
| `9e77e94` 链最终 `36cbe7c` | `29157973300` | project create/import 原子性 |
| `38e95c2` | `29159394981` | vector revision fence |
| `07b6a23` | `29162435721` | 真 Chroma + 真 PG ownership rebuild |
| `41c6b89` 链最终 `0ca10cd` | `29165249511` | dimension/CJK/trigram |
| `ff3d3fe` | `29167391929` | 全仓 API error normalization |
| `b26dfce` | `29168075722` | Outline load errors |
| `d094052` | `29168873104` | Writing load errors |
| `3e8b097` | `29169447920` | Wizard load errors |
| `aa7b64d` | `29169865091` | Detailed Outline load/detail/edit guards |
| `ef15da7` | `29170156002` | Preview list/detail errors |
| `f5ca3ae` | `29170417248` | presentation-free query hooks |
| `508399b` | `29181366394` | Characters stale errors；当前实现快照最终四 job 全绿 |

### 2.4 本地 lint 的客观边界

虽然 GitHub 最新 Frontend job 成功，但当前机器执行完整 `frontend/npm run lint` 仍返回 1：

- 22 problems：**17 errors / 5 warnings**。
- `CharactersPage.tsx`：2 个 React Compiler `preserve-manual-memoization`。
- `EntriesPage.tsx`：多处 React Compiler 手工 memoization 依赖不一致。
- `OutlineParsingSection.tsx`：render 中创建组件。
- `useOutlinePageState.ts`：effect 中同步 setState。
- `OutlinePage.tsx`、`useOutlineParsingState.ts`：hooks dependency warnings。
- `coverage/*.js`：3 个无效 eslint-disable warning。

完整输出留在本地受保护文件 `.workflow/.scratchpad/phase1_frontend_lint.txt`。这说明“CI 绿”不应被写成“本地所有 lint 债清零”。下一位维护者要先核对 CI 实际 lint 命令、忽略规则和本地依赖/产物差异。

---

## 3. 118 项口径与第一阶段真实完成度

### 3.1 原始 118 项基线

2026-07-10 的 reconciliation 基线为：

| 基线状态 | 数量 |
|---|---:|
| `partial` | 36 |
| `cleanup_only` | 30 |
| `covered_known_issue` | 19 |
| `uncovered_testable` | 9 |
| `integration_only` | 8 |
| `resolved` | 7 |
| `pg_only` | 6 |
| `refuted` | 2 |
| `covered_safety` | 1 |
| **总计** | **118** |

### 3.2 为什么现在不能给出“已完成 X/118”的简单数字

当前本地台账没有把 118 行 reconciliation CSV 全量重写成新的最终状态表；它记录的是逐提交执行证据。第一阶段：

- 第一阶段保守状态映射触及了约 38 个唯一 canonical ID；
- 其中部分 ID 完整闭环，部分只完成了某个子节点；
- 某些 commit 同时服务两个 catalog；
- 某些 catalog 用 5~6 个 commit 才闭环；
- reconciliation 中 19 个 `covered_known_issue` 行为节点已逐步毕业；这和某个时间点 pytest/vitest 收集到的 known_issue 文件/用例数量不是同一统计口径，也不代表所有 cleanup/partial/integration 项完成；
- 仍有 80 余个条目没有被第一阶段逐项重新审计和裁决。

为方便接手，文末附录给出一份**保守映射**：32 项标为阶段一有闭环证据、6 项标为部分推进、7 项沿用基线 resolved、2 项沿用终审 refuted、其余 71 项明确标为未闭环/未逐项重审。这个数字只是交接索引，不是新的正式 reconciliation；尤其那 32 项仍可能带有本文单独列出的邻接债务，下一阶段应逐条复核后再写回权威状态表。

因此本文采用三种执行状态：

1. **阶段一已闭环**：有源码、针对测试、完整门禁、独立复审、原子提交和最终绿 CI。
2. **阶段一部分推进**：只关闭了 catalog 的明确子问题，不能 overclaim 整项完成。
3. **阶段一未闭环/未重审**：保留 reconciliation 基线，不得因为邻近模块被改过就视为完成。

文末附录逐条列出 118 项，供下一阶段真正重建新的 authoritative status table。

---

## 4. 第一阶段实施原则和没有回退的终审裁决

第一阶段实际遵循的优先级是：

> 用户可见运行时缺陷 / 数据完整性 → schema 与真实 CI 地基 → auth 安全 / batch 并发 → RAG 隔离与检索 → 前端错误消费与架构债。

必须继续保持的裁决：

- 主键只用 `domain#ordinal`。
- `backend-core#2` 的“任务体双跑”高风险已被 CAS 反驳；动态探测只剩代码坏味，不能重开成原高风险问题。
- `tests#5` 的“向量实现零测试”已被反驳；facade 已有大量间接覆盖。
- lite 向量源固定为 `outline / chapter / story_memory`，不得恢复幽灵 `worldbook` 第四源。
- `backend-generation#8` 是 fallback + warning，不是把所有解析失败改成硬抛错。
- `deploy#1` 是 pgvector **或** Chroma 至少一个可用，不是二者必须同时可用。
- 保存队列 A 在途、B/C 连续编辑必须发出 `[A,C]`，不能回退为旧闭包快照。
- Admin 必须同时保留导航隐藏、前端路由守卫和后端逐请求 DB 权威校验。
- known_issue 毕业必须修实现，不可弱化测试。
- PostgreSQL/pgvector/Redis/并发问题必须以真实环境证据为准。

---

## 5. 已实现内容：按逻辑节点完整说明

### 5.1 第一批八个止血节点

#### `backend-rag-memory#2`：内存 Chroma 写读分家

- Commit：`03f263d`
- build/storage 共用一个内存 fallback store，不再写到 A、读 B。
- 增加 RLock 并发保护和 warning-once。
- facade 写后可读，且知识库隔离。
- 边界：只解决缺少 chromadb 时的 fallback；真实 Chroma/PG 的统一由后续节点完成。

#### `backend-rag-memory#5`：source order 分隔符解析

- Commit：`40fa93b`
- 修复错误正则字符类，支持空白、逗号、竖线、分号，且不再错误拆坏包含 `s` 的源名。

#### `backend-api#3`：OIDC 缺 access token fail-closed

- Commit：`db5f63a`
- OIDC 返回 2xx 但 token 缺失/null/空白时不调用 userinfo、不签 session、清临时 cookie。
- 返回稳定错误码/request ID，不再静默回跳为“像成功一样”。

#### `backend-llm-prompt#3`：lazy stream transport 错误映射

- Commit：`ecdc10f`
- Gemini 和共享 lazy stream 在连接期或迭代期出现 timeout/httpx 错误时统一映射为项目 AppError。
- 保留 cause/context，不误吞 GeneratorExit。

#### `frontend-pages#5`：保存队列 merge-latest

- Commit：`91a1363`
- 引入共享 `useQueuedSave<TArgs>`：单飞、单槽、最新快照。
- A 在途后 B/C 编辑只再保存 C；覆盖同步 throw、Promise rejection、POST→PUT 和跨编辑会话。

#### `deploy#1`：默认至少一个向量后端可用

- Commit：`eeb8255`
- Compose 默认启用 `pgvector/pgvector:pg16`。
- repair migration、部署配置和 README 对齐。
- 初期本地没有 Docker/真 PG；后续真实 PG CI 补齐了环境证据。

#### `backend-data#3`：legacy SQLite 精确识别

- Commit：`f8bae7d`
- 只有完整 canonical schema fingerprint 匹配时才自动 stamp。
- 畸形/未知/中间态库在写 Alembic 版本前 fail-closed，避免“假升级成功”。

#### `deploy#2`：SQLite→PG 迁移覆盖全部可迁移表

- Commit：`0c4cb20`
- 由源/目标反射交集和 FK 图替代硬编码表清单。
- 排除 FTS 影子表和 PG-only `vector_chunks`。
- 未知表、错误源、空源 fail-closed，包含 sequence reseed。
- 后续 `2d25b00` 再补迁移验证非零退出。

### 5.2 业务缺陷和协议节点

- `e39380c`：`backend-generation#3` 删除跨请求全局 display-name；**仅关闭状态串漏子问题**，同步/异步双编排等仍在。
- `614b502`：`backend-rag-memory#6` 给 direct-LIKE 候选 SQL 下推 limit/offset；**FTS 写而不读和 `%term%` 扫描架构仍未完全解决**。
- `8829bfc`：local register 唯一冲突窄映射 409，普通 DB 错保持 500。
- `e423aa5`：框架 404/405 进入统一 error envelope，保留 Allow header/request ID。
- `96ec224`：`X-LLM-API-Key` 命名和 4096 长度契约；`backend-api#12` 的错误码 registry、死代码等仍未完成。
- `c6bd69d`：向量活跃源统一三源，删除 vector worldbook 幽灵源。
- `493a637`：post-edit/content-optimize 共用 rewrite step，解析失败 fallback 同时写 warning。
- `f16b9f3`：Prompt Studio 的 `<OUTPUT_CONTRACT>` 和 task catalog 双向契约。
- `a70b88a`：PromptBlock provenance hash/outdated 状态机，升级保护用户自定义模板/metadata，SAVEPOINT 和事务所有权正确。
- `c967ead`：Prompts page 19 处错误盲转型先行收敛；后续 `ff3d3fe` 扩展到全仓。
- `2408cb2`：AdminGuard + 桌面/移动导航隐藏 + 后端权威防线。

### 5.3 前端 known_issue 全部毕业的三个节点

- `948f9fe`：`useProjectData` 首次暴露结构化 error/reset/latest-wins；Characters/Entries/Settings 初始错误可见。只是基础，不代表 stale refresh 全覆盖。
- `ddca1f2`：管理员创建用户后可靠单次刷新列表，避免双 GET。
- `c686dd0`：导入任务停滞超过 5 分钟显示 stalled warning/retry。

完成这些后，当时前端 known_issue 从 3 项降到 0；后续仍继续补架构和页面错误消费。

### 5.4 数据库、schema 和架构治理

#### 退役 SQLite 表

- `70ff19a`：14 张退役表确定性 JSON + SHA256 归档、preflight、事务 purge、restore。
- `7a5cff9`：migration 仅删除空退役表，非空拒绝；downgrade 重建；migrator 识别 retired set。
- 边界：这是 SQLite 归档，不等于完成 PG `pg_dump/restore` 运维演练。

#### Schema contract

- `6103dd2`：修 ORM/Alembic 类型、默认值、索引漂移。
- `c6f7d1b`：CI 增加空库 upgrade、二次 upgrade、current=head、metadata diff。
- `6ec05de`、`0ceaa98`：增加 PG schema failure 诊断。
- `6e5ca23`：修 Linux/psycopg 类型查询兼容。
- 最终真实 `pgvector:pg16` CI 全绿。

#### 大文件/重复职责下沉

- `462aa9f`：outline generation policy 从 route 下沉 service。
- `e218f62`：统一 vector storage facade/backend，清 build/storage 双实现。
- `f442392`：集中 database startup/bootstrap。
- `41b8e7e`：非法环境配置 fail-fast；仍建议复查是否所有 40+ validator 重复已真正收敛。
- `955d3ec`：route helper/领域逻辑下沉 service。

### 5.5 认证大项 `backend-api#4`

- `863e705`：管理员创建用户唯一冲突窄映射 409。
- `8ffa045`：本地注册/重置/管理员等新密码统一 8..256 和空白规则。
- `c60e47b`：`AdminUserDep` 声明式注入，统一 401/403/OpenAPI。
- `9239237`：OIDC discovery monotonic TTL、按 URL 分区、Condition singleflight、失败不缓存。
- `35fdd77`：851 行 auth route 拆成 session/local/OIDC/admin routers 和 services，旧 facade 仅兼容 re-export。
- `320dfb4`：Redis Lua 原子 account+IP 双桶限流；prod Redis 不可用 fail-closed 503；dev 有界内存 fallback；可信代理 CIDR/XFF；安全 Retry-After。
- `4a4a3ed`、`66696d6`：增加 CI failure annotation 和 Linux deploy shell 期望修正。
- 真实 Redis CI 已绿。

**仍需复审**：`backend/app/services/authentication/rate_limit.py` 直接 import 私有 `_get_signing_key`。这不是当前功能失败，但破坏稳定模块边界，应提供公开、域分离的 rate-limit identity HMAC 接口。

### 5.6 GET 无副作用、batch 配额和状态机

#### `backend-api#5`

- `afb77e1`：11 个普通 GET 改为 viewer，无隐式创建/升级/写库。
- 新增显式 `POST /prompt_presets/sync_builtin_defaults`。
- generation 未同步时 fail-closed，不再偷偷 ensure。
- `11b36a1` 修真 PG sync fixture 顺序。

#### `backend-api#6`

- `a727201`：create/resume/retry/skip 使用统一原子配额准入；PG guard row lock / SQLite `BEGIN IMMEDIATE`；worker provider drift fail-closed。
- `78efcfc`：真 PG 并发 fixture ID 修正。
- `b011f11`：状态机/command service 下沉，route 大幅变薄；enqueue failure 补偿，cancel/pause 共享锁协议。

#### Worker 独占 claim

- `ff0de6e`：queued→running CAS，重复投递只能一个 worker 真执行。
- `e294639`、`2afd66e`、`957bd7c`：Linux/SQLite fixture 逐步收敛。
- **未包含** stale-running recovery；worker 崩溃后如何安全回收仍需单独节点。

### 5.7 数据完整性、迁移和会话安全

#### 章节 replace 原子性：`backend-api#8a`

- `f384ed9`：DELETE 旧章节和 INSERT 新章节同一事务，仅一次 commit；插入失败旧章节完整回滚。
- `d6c023a`、`158fdb8`：真 PG active outline FK 和排序 fixture 修复。

#### Migrator fail-closed

- `2d25b00`：count/digest/composite/self/schema-aware FK 验证失败时 stderr + 非零退出，不打印成功。
- 报告原子写入、resume drift 验证、单 REPEATABLE READ snapshot、UTC 和 secret 脱敏。

#### 禁用用户会话失效

- `e45b441`：`disabled_at/session_invalid_before/session_version`；session v2 绑定 iat+version。
- local/OIDC/admin/refresh/新 SSE 握手统一 enabled principal。
- **边界**：已经打开的 SSE 不会在管理员禁用用户后中途即时断开。

#### Generation 显式 prompt 同步

- `f615f12`：post-edit、content-optimize、chapter plan、plan_first、batch 都不再隐式 ensure/create/upgrade。
- 缺配置时无 DB/LLM 副作用并明确 fail-closed。

### 5.8 项目创建、导入和向量重建事务

- `9e77e94`：Project、owner、6 个 prompt resources、default KB 单事务；DB 失败整单回滚；post-commit vector failure 结构化 degraded。
- `36cbe7c`：import post-commit vector session 绑定修复。
- `38e95c2`：`vector_dirty_revision` fencing；旧 build 不能清掉新 dirty，worker event/result/clear 同事务。

这些覆盖了原 `backend-api#8` 点名的 chapters、project create/import、vector rebuild 事务/revision 边界；台账在 `38e95c2` 最终 CI 后将该 catalog 判为闭环。本文仍把“未知邻接事务路径”列为持续 review 风险，但不再用这种泛化风险反向否定原 catalog 的闭环证据。

### 5.9 RAG/向量后期重构

#### `backend-rag-memory#3`：KB 隔离

- `a868f76`：`vector_chunks` 带 `kb_id`，delete/rebuild/query 按 project+KB 隔离，多 KB 不静默折成 1。
- `8633d09`、`7ac4499`、`a0e1082`：修 legacy PG fixture。
- 真实 pgvector schema/migration/query 已绿。

#### `backend-rag-memory#4`：所有权和重复 embedding

- `07b6a23`：default KB 拥有原生三源+default/null docs；custom KB 只拥有分配给自己的 done source docs。
- bulk coordinator 对相同文本只 embedding 一次。
- 空 KB 替换会清旧 index；retry 只 purge 目标 doc/KB。
- PG replace 事务化；Chroma 有进程内 snapshot/restore。
- 真 Chroma 在 Ubuntu CI 通过。
- 边界：不宣称 Chroma 在进程崩溃/断电时具备数据库级原子性。

#### `backend-rag-memory#7`：维度和中文检索

- `41c6b89`：expected dimension 贯穿 env/settings/profile/API/bundle；校验空批、数量、统一维度和 expected 维度。
- `01c0f3e`：PG CJK exact substring/ILIKE/similarity 评分。
- `0ca10cd`：trigram index plan 测试隔离。
- 边界：相同维度但 provider/model identity 发生漂移的全局 fingerprint 尚未解决。

**重要待复核冲突**：当前 `vector_storage._prefer_pgvector()` 在显式 `VECTOR_BACKEND=pgvector` 但 `_pgvector_ready()==False` 时返回 false，后续可能走 Chroma；历史 migration `4858b08a6519` 在扩展不可用或 `CREATE EXTENSION` 失败时直接 return。朋友复审明确要求审计“显式 pgvector 必须硬失败”的实际端到端语义。不要仅凭维度测试就宣称这个问题已经彻底关闭。

### 5.10 全仓前端错误归一化

- `ff3d3fe`：共享 `toApiError(unknown, fallback)`、presentation/toast helper；清理真实 `as ApiError`、局部 normalizer 和重复 message/code 拼接。
- 保留 wire payload 层必要的类型判定。
- 加静态源码契约，防止 unsafe assertion 和局部 normalizer 回潮。
- 边界：`apiClient` 与 `sseClient` 仍各自解析 wire error envelope；这一底层重复属于独立 `frontend-arch#10` 风险。
- 更广义的页面 toast 样板仍多，`frontend-pages#4` 不能只因盲转型清零就 full close。

### 5.11 `frontend-arch#8` 页面错误消费链

#### Outline：`b26dfce`

- 首次 blocking error、stale warning、真实 empty state 分开。
- dirty editor 禁止危险 retry 覆盖。

#### Writing：`d094052`

- metadata/list/detail 各自区分 initial/stale/empty。
- dirty 状态阻止危险 reload；Promise rejection 收口。

#### Wizard：`3e8b097`

- wizard/chapter errors、`allSettled` reload、stale progress warning。
- NextBar 出错时不假装可继续；dirty owner 禁 retry。

#### Detailed Outline：`aa7b64d`

- list/detail 独立 loading/error/hasData/hasLoaded、独立 request sequence/scope guard。
- stale 失败保留数据，initial 失败 blocking。
- 编辑期间禁 reload 和跨卷切换，避免本地内容静默丢失。
- 删除当前卷完整 reset detail state。
- 残留：非编辑时，服务端外部删除当前选中卷后，普通 list refresh 不会自动 reconcile 旧 selected detail。

#### Preview：`ef15da7`

- 章节列表失败不再显示“暂无章节”；正文失败不再显示“（空）”。
- list/body initial blocking、stale preserve、inline retry。
- 自动 load 和 retry rejection 不产生 unhandled Promise。

#### Query hooks presentation-free：`f5ca3ae`

- `useProjectData/useChapterMetaList/useChapterDetail` 删除 Toast 和 `toastOnError`。
- Hook 只负责 state/data/error/retry，页面负责 presentation。
- 静态测试禁止这些 hook 重新 import toast/presentation。

#### Characters：`508399b`

- blocking 与 stale refresh warning 分离。
- stale 时保留角色卡、成功空态和筛选空态。
- drawer dirty 表单不会被列表 retry 覆盖。

**仍未完成**：Entries stale refresh 和 Settings stale refresh/dirty 策略，因此 `frontend-arch#8` 仍应保持“部分推进”，不要 full close。

---

## 6. 当前明确未完成、只完成一部分或可能仍有问题的事项

### P0/P1：下一步最应该做

1. **Entries stale refresh UI**
   - 当前只处理 `data===null` 的初始错误。
   - `data!==null && error` 时没有 warning/retry。
   - stale 空数组或筛选无结果可能形成静默/空白。
   - 必须像 Characters 一样保留旧列表和 drawer dirty 内容。

2. **Settings stale refresh + dirty 保护**
   - 当前只暴露 blockingLoadError。
   - 已有 settings 后 refresh 失败完全不可见。
   - 与 Characters 不同，成功 reload 可能覆盖 settings/project dirty form；retry 必须在 dirty 时阻止或先确认保存/放弃。

3. **Rate limiter 私有 `_get_signing_key`**
   - `rate_limit.py` 直接依赖 `auth_session.py` 私有函数。
   - 建议提供公开、域分离、稳定的 HMAC identity API，并补兼容/轮换测试。

4. **历史 `4858b08a6519` migration 与显式 pgvector 硬失败**
   - migration 在扩展不可用/无权限时 return，可能把 schema 缺失记录为 revision 成功。
   - 显式 `VECTOR_BACKEND=pgvector` 的运行时 readiness 不足可能仍回落 Chroma。
   - 应建立 fresh PG、extension unavailable、permission denied、显式 backend startup/API 的真实测试，明确 hard-failure 位置和错误信息。

### 高价值后续

5. `apiClient` / `sseClient` wire error envelope 解析统一，避免 code/message/request_id/details 漂移。
6. 修完整仓 React Compiler/hooks lint 债，并核对为何本地 `npm run lint` 红而 CI job 绿。
7. Batch stale-running recovery：worker 崩溃、租约/心跳、重复恢复和 exactly-once side effect。
8. 禁用用户后已打开 SSE 的中途撤销策略。
9. Embedding provider/model identity fingerprint；不仅校验维度。
10. Detailed Outline 外部删除 selected reconcile。
11. Search FTS 真正读路径：当前只是 direct-LIKE 有界，FTS/search_rebuild 仍可能“只写不读”。
12. `frontend-pages#4` 的剩余 toast 样板和 presentation 重复；不要只统计 `as ApiError`。
13. `useChapterMetaList.toastPolicy.test.tsx` 文件名/描述已过时，功能已改为 presentation-free，可做纯清理重命名。
14. PG 运维层归档/恢复演练：SQLite retired table archive 不等价于 PG `pg_dump/restore`。

### 不应被误认为已解决的边界

- “CI 四绿”不等于 118 项完成，只证明当前门禁覆盖的行为未回归。
- “known_issue 为 0”不等于 cleanup/integration/PG-only/partial 全部完成。
- “显式 dimension contract 已有测试”不等于 pgvector availability hard-failure 已完成。
- “worker 独占 claim”不等于 worker 崩溃后的 stale-running recovery 已完成。
- “禁用用户新请求被拒绝”不等于已建立的长连接立即被切断。
- “Chroma snapshot/restore”不等于跨进程崩溃原子性。
- “全仓无真实 `as ApiError`”不等于所有错误 presentation 已统一。

---

## 7. 建议独立 review 的重点

下一轮 reviewer checklist 至少包括：

1. **事务边界**：DELETE+INSERT、跨 session、post-commit side effect、失败后旧数据是否完整。
2. **并发独占**：claim/CAS 是否只保护状态还是也保护外部副作用；恢复路径会不会双执行。
3. **fail-closed**：迁移、复制、schema 验证、显式 backend 不可用时是否非零/硬失败。
4. **overclaim**：一个子节点通过后是否错误关闭整个 catalog。
5. **真实环境**：PG/pgvector/Redis/Linux/Chroma 是否真的运行，而非 SQLite/mock 替代。
6. **错误状态**：initial error、stale error、empty state 是否混淆；retry rejection 是否 unhandled。
7. **dirty 数据保护**：reload 是否覆盖本地表单/编辑器；是否需要 retryBlockedReason/确认框。
8. **secret 与日志**：API key、token、DSN、异常 details 是否泄漏。
9. **兼容与迁移**：legacy rows、upgrade/downgrade、无扩展/无权限、旧 cookie/旧 bundle。
10. **CI 真实性**：历史失败是否被正确修复，还是通过弱化断言/跳过条件假绿。

特别建议再次 review：

- `backend/app/services/authentication/rate_limit.py`
- `backend/alembic/versions/4858b08a6519_add_pgvector_vector_chunks.py`
- `backend/app/services/vector_storage.py::_prefer_pgvector`
- `frontend/src/pages/EntriesPage.tsx`
- `frontend/src/pages/settings/useSettingsPageState.ts`
- `frontend/src/services/apiClient.ts` 与 `sseClient.ts`
- `frontend/src/pages/CharactersPage.tsx` / `EntriesPage.tsx` 的 React Compiler 报错
- batch worker stale-running 和 SSE session revocation 生命周期

---

## 8. 下一阶段推荐执行顺序

### Wave 0：先同步阶段一状态，防止重复实施和 overclaim

1. 把阶段一触及的约 38 个 canonical ID 写回一份新的 execution reconciliation。
2. 每行至少记录 `execution_status / commits / tests / CI run / reviewer / residual`。
3. 先解决本文附录与旧 baseline 的差异，再开始新的大规模实现；剩余 80 余项可后续逐波重审。

### Wave 1：先处理后端 fail-closed 硬语义

4. 4858 migration fail-closed + `VECTOR_BACKEND=pgvector` 显式不可回落。
5. 真实 PG extension unavailable/permission denied 启动和 migration 测试。
6. rate limiter 公共 signing/identity API。

### Wave 2：把当前前端 load-error 链收尾

7. Entries stale warning/retry + dirty drawer 回归。
8. Settings stale warning/retry + dirty retry block/confirm。
9. 重新审计 `frontend-arch#8` 所有生产调用，只有全部页面可见消费后才 full close。

### Wave 3：底层前端错误和 lint 架构

10. 统一 HTTP/SSE wire envelope parser。
11. 清 React Compiler/hooks lint 债，并让本地与 CI lint 命令/忽略范围一致。
12. 继续 `frontend-pages#4` toast/presentation 样板收敛。

### Wave 4：生命周期和恢复语义

13. stale-running batch recovery。
14. 已打开 SSE 的用户禁用/取消传播。
15. embedding model/provider fingerprint 与重建策略。

### Wave 5：重新对账 118 项

16. 在 Wave 0 的已触及项同步基础上，逐行重审其余 reconciliation CSV。
17. 对未触及的 cleanup/integration/PG-only 项动态排序。
18. 只有 118 行都为 closed/refuted/resolved 且证据可复现时，才能调用整体完成。

---

## 9. 接手后的标准执行模板

每个原子节点继续使用：

1. 检查 `git status --short --branch`、HEAD、remote。
2. 读 catalog 原文、现有测试和至少 3 个相似模式。
3. 明确整项与子节点边界，先写失败证据/针对测试。
4. 实现 boring、可回滚方案。
5. 定向测试 → test TypeScript/Ruff → build → 完整门禁。
6. 真实 PG/Redis/Chroma/Linux 条件必须真实跑。
7. 独立 reviewer，强制检查事务/并发/fail-closed/overclaim。
8. `git diff --check`，只 `git add <specific-files>`。
9. 一个语义节点一个 commit，立即 push `origin/dev`。
10. 等 GitHub CI；若红，记录失败原因并用独立 follow-up commit 修复，不 amend/force push。
11. 更新 `.workflow/.scratchpad/remediation_progress.md`，明确“闭环”或“部分边界”。

---

## 10. 第一阶段 76 个 commit 完整索引

下表由 `git log --reverse df31091..508399b` 直接生成；它是“没有遗漏 commit”的索引。具体实现语义以上文逻辑节点为准。

<!-- COMMIT_INDEX_BEGIN -->
| # | Commit | Date | Subject |
|---:|---|---|---|
| 1 | `03f263d` | 2026-07-10 | fix(rag): share in-memory chroma fallback |
| 2 | `40fa93b` | 2026-07-10 | fix(rag): parse source order separators |
| 3 | `db5f63a` | 2026-07-10 | fix(auth): fail closed on missing OIDC token |
| 4 | `ecdc10f` | 2026-07-10 | fix(llm): wrap lazy stream transport errors |
| 5 | `91a1363` | 2026-07-10 | refactor(frontend): share queued save hook |
| 6 | `eeb8255` | 2026-07-10 | fix(deploy): enable pgvector by default |
| 7 | `f8bae7d` | 2026-07-10 | fix(db): identify legacy sqlite revisions exactly |
| 8 | `0c4cb20` | 2026-07-10 | fix(deploy): migrate all portable sqlite tables |
| 9 | `e39380c` | 2026-07-10 | fix(generation): isolate agent display names |
| 10 | `614b502` | 2026-07-10 | fix(search): bound direct candidate queries |
| 11 | `8829bfc` | 2026-07-10 | fix(auth): map registration conflicts |
| 12 | `e423aa5` | 2026-07-10 | fix(core): unify framework error envelopes |
| 13 | `96ec224` | 2026-07-10 | fix(api): bound detailed outline API key header |
| 14 | `c6bd69d` | 2026-07-10 | fix(rag): unify active vector sources |
| 15 | `493a637` | 2026-07-10 | fix(generation): unify rewrite fallback handling |
| 16 | `f16b9f3` | 2026-07-11 | fix(prompt): preserve studio output contracts |
| 17 | `a70b88a` | 2026-07-11 | fix(prompt): preserve customized resource blocks |
| 18 | `c967ead` | 2026-07-11 | fix(frontend): normalize prompts page errors |
| 19 | `2408cb2` | 2026-07-11 | fix(frontend): guard admin user management |
| 20 | `948f9fe` | 2026-07-11 | fix(frontend): expose project query errors |
| 21 | `ddca1f2` | 2026-07-11 | fix(frontend): refresh admin users after create |
| 22 | `c686dd0` | 2026-07-11 | fix(frontend): detect stalled imports |
| 23 | `70ff19a` | 2026-07-11 | feat(data): archive retired sqlite tables |
| 24 | `7a5cff9` | 2026-07-11 | refactor(data): retire unused legacy tables |
| 25 | `6103dd2` | 2026-07-11 | fix(db): reconcile schema metadata |
| 26 | `c6f7d1b` | 2026-07-11 | ci(db): enforce schema contract |
| 27 | `6ec05de` | 2026-07-11 | ci(db): report postgres schema failures |
| 28 | `0ceaa98` | 2026-07-11 | ci(db): include postgres failure trace |
| 29 | `6e5ca23` | 2026-07-11 | fix(ci): query postgres types safely |
| 30 | `462aa9f` | 2026-07-11 | refactor(generation): move outline policy to services |
| 31 | `e218f62` | 2026-07-11 | refactor(rag): unify vector storage backend |
| 32 | `f442392` | 2026-07-11 | refactor(bootstrap): centralize database startup |
| 33 | `41b8e7e` | 2026-07-11 | refactor(config): reject invalid environment values |
| 34 | `955d3ec` | 2026-07-11 | refactor(api): move route helpers into services |
| 35 | `863e705` | 2026-07-11 | fix(auth): map user creation conflicts |
| 36 | `8ffa045` | 2026-07-11 | fix(auth): align new password validation |
| 37 | `c60e47b` | 2026-07-11 | refactor(auth): inject admin authorization |
| 38 | `9239237` | 2026-07-11 | feat(auth): cache oidc discovery metadata |
| 39 | `35fdd77` | 2026-07-11 | refactor(auth): split authentication modules |
| 40 | `320dfb4` | 2026-07-11 | feat(auth): enforce distributed rate limits |
| 41 | `4a4a3ed` | 2026-07-11 | ci(test): report backend failures |
| 42 | `66696d6` | 2026-07-11 | test(deploy): expect trusted proxy startup |
| 43 | `afb77e1` | 2026-07-11 | fix(api): make configuration reads side effect free |
| 44 | `11b36a1` | 2026-07-11 | test(pg): order prompt sync fixtures |
| 45 | `a727201` | 2026-07-11 | fix(batch): serialize quota admission |
| 46 | `78efcfc` | 2026-07-11 | test(pg): bound batch race fixture ids |
| 47 | `b011f11` | 2026-07-11 | refactor(batch): centralize task commands |
| 48 | `f384ed9` | 2026-07-11 | fix(chapters): make bulk replacement atomic |
| 49 | `d6c023a` | 2026-07-11 | test(pg): respect active outline foreign key |
| 50 | `158fdb8` | 2026-07-11 | test(pg): decouple replacement ids from sort order |
| 51 | `a868f76` | 2026-07-11 | fix(rag): isolate pgvector knowledge bases |
| 52 | `8633d09` | 2026-07-11 | test(pg): populate legacy fixture timestamps |
| 53 | `7ac4499` | 2026-07-11 | test(pg): populate legacy admin flag |
| 54 | `a0e1082` | 2026-07-11 | test(pg): complete duplicate vector fixture |
| 55 | `ff0de6e` | 2026-07-11 | fix(batch): claim worker execution exclusively |
| 56 | `e294639` | 2026-07-11 | test(batch): create worker event tables explicitly |
| 57 | `2afd66e` | 2026-07-11 | test(batch): use canonical table setup |
| 58 | `957bd7c` | 2026-07-11 | test(batch): isolate sqlite worker claims |
| 59 | `2d25b00` | 2026-07-11 | fix(migrate): fail closed on verification errors |
| 60 | `e45b441` | 2026-07-11 | fix(auth): revoke disabled user sessions |
| 61 | `f615f12` | 2026-07-11 | fix(generation): require explicit prompt synchronization |
| 62 | `9e77e94` | 2026-07-11 | fix(projects): initialize projects atomically |
| 63 | `36cbe7c` | 2026-07-11 | test(import): bind post-commit vector session |
| 64 | `38e95c2` | 2026-07-12 | fix(vector): fence rebuild state updates |
| 65 | `07b6a23` | 2026-07-12 | refactor(rag): rebuild knowledge bases by ownership |
| 66 | `41c6b89` | 2026-07-12 | fix(rag): enforce embedding dimension contract |
| 67 | `01c0f3e` | 2026-07-12 | fix(rag): score exact CJK substrings |
| 68 | `0ca10cd` | 2026-07-12 | test(pg): isolate trigram index plan |
| 69 | `ff3d3fe` | 2026-07-12 | refactor(frontend): normalize api errors consistently |
| 70 | `b26dfce` | 2026-07-12 | fix(outline): surface project load failures |
| 71 | `d094052` | 2026-07-12 | fix(writing): distinguish load failures from empty state |
| 72 | `3e8b097` | 2026-07-12 | fix(wizard): expose progress load failures |
| 73 | `aa7b64d` | 2026-07-12 | fix(outline): expose detailed outline load failures |
| 74 | `ef15da7` | 2026-07-12 | fix(preview): expose chapter load failures |
| 75 | `f5ca3ae` | 2026-07-12 | refactor(frontend): keep query hooks presentation free |
| 76 | `508399b` | 2026-07-12 | fix(characters): surface stale load failures |
<!-- COMMIT_INDEX_END -->

---

## 11. 118 项逐条阶段状态附录

说明：

- `baseline` 是 2026-07-10 reconciliation 的原始状态，不会因第一阶段提交自动改变。
- `phase1` 是本文基于 commit、测试、CI、review 台账给出的保守执行判断。
- 最后一列原样保留 2026-07-10 的原始问题描述和当时 remediation direction；对于 phase1 已闭环的行，其中写着的“下一步”通常已经由第一阶段完成，**不是当前待办**。当前真实 residual 以本文第 6～8 节和 phase1 列为准。
- “已闭环”仍建议下一阶段抽查证据；“部分推进”绝不能关闭 catalog；“未重审”必须回到原始 gap。

<!-- CATALOG_118_BEGIN -->
| # | Canonical ID | Severity | Baseline | Phase 1 conservative status | Original audit gap / historical remediation direction |
|---:|---|---|---|---|---|
| 1 | `backend-api#1` | critical | `cleanup_only` | 已闭环主要 route/service 分层：462aa9f（与 generation#1 共用） | Service→Route 反向依赖：outline 生成域分层倒置；下一步：迁出策略到 service 后用现有 outline route/service 绿色测试保证行为不变。 |
| 2 | `backend-api#2` | high | `cleanup_only` | 已闭环主要 helper 下沉：955d3ec | *_route_helpers 拆分是分层失败的制度化，同类需求两套规范并存；下一步：按域迁入 service；避免新增针对 route 私有 helper 的测试。 |
| 3 | `backend-api#3` | high | `covered_known_issue` | 已闭环：db5f63a（OIDC token fail-closed） | OIDC 回调静默失败：token 缺失时用户被无提示重定向且未登录；下一步：改为 _fail("OIDC_TOKEN_MISSING") 后毕业。 |
| 4 | `backend-api#4` | high | `partial` | 主要 catalog 已闭环：认证冲突/密码/DI/cache/分层/Redis限流；私有 signing key 残留 | auth.py 847 行上帝文件：注册竞态、弱密码策略、无限流、阻塞外呼无缓存；下一步：修 IntegrityError 映射后毕业；另补登录限流与 discovery 缓存调用次数测试。 |
| 5 | `backend-api#5` | high | `partial` | 已闭环：afb77e1 + 11b36a1（GET无写、显式sync） | 鉴权粒度不一致 + GET 端点写库副作用；下一步：对 GET 前后 DB snapshot 断言无写入；对升级使用显式 POST/事务并补并发测试。 |
| 6 | `backend-api#6` | high | `partial` | 已闭环主配额/状态机：a727201/78efcfc/b011f11；stale-running另审 | 批量生成配额检查全表扫描 + check-then-act 竞态；731 行状态机在路由层；下一步：补并发创建配额集成测试与 SQL 查询界限/索引断言；状态机重构继续复用现有护栏。 |
| 7 | `backend-api#7` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 全同步技术栈：SSE 长流占满线程池并长期持有 DB 连接；下一步：PG service job 中并发打开长流，检查连接池占用与事务在 LLM 调用前释放。 |
| 8 | `backend-api#8` | high | `partial` | 阶段一已闭环原 catalog：f384ed9/158fdb8、9e77e94/36cbe7c、38e95c2；未知邻接事务风险单列持续 review | 事务边界破碎：多段 commit 非原子、replace 先删后插可丢数据、双 session 并用；下一步：在插入阶段注入异常，断言旧章节/旧配置仍完整；PG 下验证事务边界。 |
| 9 | `backend-api#9` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 复制粘贴泛滥：同一函数体最多重复 6 处；下一步：抽共享函数并复用各公开 route happy-path。 |
| 10 | `backend-api#10` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 上帝函数：settings 配置合并 235 行、admin 用户列表 7 个聚合查询内联；下一步：拆 service/query object，保留公开响应字段与分页测试。 |
| 11 | `backend-api#11` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 路由层实现上游 HTTP 客户端并击穿服务私有边界；下一步：迁移到 service/client 后继续 mock 外部依赖，不测试私有 helper。 |
| 12 | `backend-api#12` | medium | `partial` | 部分推进：96ec224 仅 Header 长度/命名；错误码 registry/死代码未做 | 错误码 ad hoc、约定漂移与死代码；下一步：补 max_length=4096 后毕业；建立错误码 registry 唯一性/公开端点契约测试。 |
| 13 | `backend-core#1` | high | `partial` | 已闭环 schema contract：6103dd2/c6f7d1b/6ec05de/0ceaa98/6e5ca23 | ORM 模型与 Alembic 迁移靠注释人工对齐，schema drift 已在生产发生且无 CI 防线；下一步：CI 增加空库 upgrade head + alembic check/metadata diff，覆盖任意列/默认值/可空性漂移。 |
| 14 | `backend-core#2` | refuted（余留 medium 代码坏味） | `refuted` | 基线已反驳；阶段一未重新实现，保留终审裁决并复核证据 | dev 任务队列后端逐次动态探测可静默漂移，与看门狗组合存在任务重复执行风险；下一步：只保留动态探测/私有字段访问的代码整洁度改进，不再建立双跑 known_issue。 |
| 15 | `backend-core#3` | high | `cleanup_only` | 已闭环：f442392（bootstrap 集中） | bootstrap 逻辑在 entrypoint.sh 与 main.py 双份手工维护，含逐字复制的 admin 密码容错块；下一步：抽共享 app.bootstrap 后复用现有行为测试；如保留 shell 入口，再加容器启动 smoke。 |
| 16 | `backend-core#4` | high | `partial` | 已闭环非法配置 fail-fast：41b8e7e；validator 重复仍建议复查 | config.py 40+ 个复制粘贴的 field_validator，约 450/619 行为同构样板且静默吞非法配置；下一步：补表驱动非法 env 测试（abc、越界、空白），再抽 Annotated/通用 validator。 |
| 17 | `backend-core#5` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | deps.py 实体守卫 12 连复制 + 5 个死代码别名 + 死常量 LOCAL_USER_ID；下一步：删除死符号并用静态引用扫描/ruff 防回潮。 |
| 18 | `backend-core#6` | medium | `covered_known_issue` | 已闭环：e423aa5（404/405 envelope） | 未注册 StarletteHTTPException 处理器，404/405 响应绕过统一错误契约；下一步：注册 StarletteHTTPException handler 后转入 safety board。 |
| 19 | `backend-core#7` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | main.py 约六成为叙事性注释且已与事实脱节（注释漂移）；下一步：删除重复叙述；可用架构文档审核而非单测。 |
| 20 | `backend-core#8` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 会话签名密钥复用 Fernet 加密密钥，dev 随机密钥每进程独立导致会话互不互认；下一步：补显式 session signing key 派生测试和双进程同 key cookie 互认 smoke。 |
| 21 | `backend-core#9` | low | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | settings 与 engine 均为 import 期副作用，DB session 管理双轨制（51 处直接 SessionLocal）；下一步：收敛 session 注入后以 import smoke 和 session 生命周期测试护航。 |
| 22 | `backend-core#10` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | task_queue 的 kind→函数分发表在 inline 与 rq 两个后端里复制粘贴；下一步：抽单一 registry，并保留两后端参数化测试。 |
| 23 | `backend-core#11` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | inline 线程池无关闭/排空机制，daemon 线程随进程硬杀丢任务；下一步：新增子进程启动→入队→SIGTERM/正常退出→断言 drain/状态一致的集成测试。 |
| 24 | `backend-core#12` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | /api/health 无鉴权暴露基础设施拓扑且每次请求触发 Redis 实时探测；下一步：拆 liveness/readiness/admin diagnostics，并加匿名字段契约与探测缓存/调用次数测试。 |
| 25 | `backend-data#1` | critical | `cleanup_only` | 已闭环代码节点：70ff19a + 7a5cff9；PG运维演练另审 | 13 张死表 + structured_memory 整文件死模型，autogenerate 会提议删表；下一步：先确认数据保留策略，再出显式 drop/archive migration 与 schema snapshot。 |
| 26 | `backend-data#2` | critical | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | project_settings 上帝表 + ORM 为默认值唯一来源的漂移事故模式（b61575a 所修 bug 的根因仍在）；下一步：同 backend-core#1：metadata↔migration 全量对账；拆领域表并集中 get_or_create。 |
| 27 | `backend-data#3` | high | `covered_known_issue` | 已闭环：f8bae7d（legacy fingerprint） | 迁移历史事故链：字符串时间戳→全量转换→数据修复迁移→启发式 stamp head；下一步：移除/收紧启发式并验证真实 schema 后毕业。 |
| 28 | `backend-data#4` | high | `pg_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | pgvector 条件迁移不确定：扩展缺失时静默跳过但版本已标记 applied；下一步：PG job 模拟 extension 不可用/恢复，断言 revision 不误成功或能可靠补建。 |
| 29 | `backend-data#5` | high | `pg_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | vector_chunks 无 ORM 模型且 embedding vector(1536) 硬编码维度；下一步：真实 PG 用多维模型和 alembic check，或改可配置维度/标准化模型表。 |
| 30 | `backend-data#6` | high | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 双方言（SQLite/Postgres）负担渗透所有迁移与检索服务；下一步：CI 建 SQLite + PG 双矩阵，执行 migration head 与关键检索契约。 |
| 31 | `backend-data#7` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | LLM 参数块重复 9 份、校验器逐字复制 4 处，VectorRagProfile 与 ProjectSettings 列块重复；下一步：抽 mixin/value object 后跑现有 route/schema 测试。 |
| 32 | `backend-data#8` | medium | `pg_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 索引与真实查询模式不匹配（generation_runs 最突出）；下一步：PG 大样本 EXPLAIN ANALYZE，断言关键过滤/排序使用目标复合索引。 |
| 33 | `backend-data#9` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 39 个 *_json Text 列，JSON 存字符串且解析失败静默兜底；下一步：建立 typed JSON facade 与坏数据 warning；按模型参数化 round-trip/损坏样本。 |
| 34 | `backend-data#10` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 时间戳/软删除策略不一致，默认值全在客户端且 utc_now 截断微秒；下一步：统一 UTC server/client policy，并以模型矩阵验证 create/update/soft-delete。 |
| 35 | `backend-data#11` | medium（建议降 low） | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | users.password_hash 死列（认证已全部迁至 user_passwords）；下一步：确认无回滚依赖后迁移删除；可用 schema snapshot 防回潮。 |
| 36 | `backend-data#12` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 类型注解与可空性矛盾、Integer 当 Boolean、命名/风格不一致；下一步：启用 mypy/pyright + Alembic metadata 校验；按迁移批次修正。 |
| 37 | `backend-generation#1` | high | `cleanup_only` | 已闭环主要 route/service 分层：462aa9f | outline_generation 服务层反向依赖 API 路由层，891 行核心策略放错层；下一步：迁移策略时只新增 service 直接测试，不继续深挖 route 私有 helper。 |
| 38 | `backend-generation#2` | high | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 五条管线复制粘贴同一套 SSE 流式执行逻辑且已漂移出不一致行为；下一步：抽共享 streaming executor，并以同一契约矩阵覆盖五条管线。 |
| 39 | `backend-generation#3` | high | `covered_known_issue` | 部分推进：e39380c 仅修跨请求 display-name；双编排等未做 | outline_parsing_agent 过度工程：同步/异步双 300 行编排重复 + 跨请求全局可变状态；下一步：改为请求上下文局部状态后毕业。 |
| 40 | `backend-generation#4` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | run_project_task 上帝函数：任务分发器内联全部任务业务与 75 行错误文案映射；下一步：拆 kind handlers + 错误注册表，保留公开状态转换测试。 |
| 41 | `backend-generation#5` | high | `covered_safety` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 章节编号与建章逻辑三处复制且存在编号冲突隐患；下一步：重构三处建章逻辑时必须保持该组绿色。 |
| 42 | `backend-generation#6` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | JSON 提取/修复存在 4 套独立实现；下一步：抽统一 parser/repair facade，参数化旧入口保持行为。 |
| 43 | `backend-generation#7` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 重试逻辑四套并存，425 行章节流生成器嵌套 6 层；下一步：定义 retry policy matrix，按超时/4xx/解析失败/取消参数化所有入口。 |
| 44 | `backend-generation#8` | medium | `covered_known_issue` | 已闭环：493a637（fallback + warning） | post_edit 与 content_optimize 两个 85 行 step 近全同且静默吞 LLM 配置错误；下一步：两 step 共用实现并记录 warning 后毕业；测试函数名后续可改为 records_config_warning。 |
| 45 | `backend-generation#9` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 可取消性缺口：运行中任务与单章生成不可中断；下一步：真实 worker/假慢 LLM 集成：发起→取消→确认上游停止、DB 状态 canceled、无后续写入。 |
| 46 | `backend-generation#10` | medium | `partial` | 部分推进：batch command/state/claim 已重构；双状态机与stale recovery需最终复核 | BatchGenerationTask 与 ProjectTask 双状态机手工双写易分叉；下一步：引入单一状态转换函数/DB 约束并保留矩阵测试。 |
| 47 | `backend-generation#11` | low | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | task_queue 探测逻辑在热路径且模块级缓存无锁、watchdog 全表扫；下一步：缓存启动期决策、watchdog 分页/索引；如优化再加调用次数与查询界限测试。 |
| 48 | `backend-generation#12` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 三套 max_tokens 预算口径互相矛盾（其中两处又是复制粘贴）；下一步：建立跨 provider/pipeline 的统一预算函数与参数化边界测试。 |
| 49 | `backend-llm-prompt#1` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | provider 层 8 份近似拷贝：同步/流式 × 4 provider 的端点探测、降级阶梯、SSE 解析全部重复（~千行）；下一步：抽 provider core 后用现有跨 provider 契约测试防漂移。 |
| 50 | `backend-llm-prompt#2` | high | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 多层重试相乘（provider 降级×端点探测×chat↔responses 互退×服务级3次），并静默丢弃用户采样参数；下一步：增加 transport call counter（总 POST≤约定值）与 adjustments 审计断言。 |
| 51 | `backend-llm-prompt#3` | high | `covered_known_issue` | 已闭环：ecdc10f（lazy stream transport） | 流式/非流式错误契约不对称：gemini 流式 generator 无 httpx 异常捕获，原始 ReadTimeout 逃逸；下一步：对齐其余 provider 异常映射后毕业。 |
| 52 | `backend-llm-prompt#4` | high | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 依赖 httpx 私有 API httpx._client.StreamContextManager 并手工 __enter__/__exit__；下一步：改用公开 client.stream()；加 grep/architecture rule 禁止 httpx._* import/annotation。 |
| 53 | `backend-llm-prompt#5` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | render_preset_for_task 上帝函数：约 500 行、7 元组返回、预算裁剪逻辑三份重复、跨模块引用私有函数；下一步：补 service 直接参数化测试，再拆结果 dataclass 与裁剪阶段。 |
| 54 | `backend-llm-prompt#6` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | chapter_generate v3/v4 双版本并存：13 个模板字节级重复、8 个已分叉、旧项目无迁移路径；下一步：对 v3 自定义/未自定义项目执行升级矩阵，断言幂等且不丢用户改动。 |
| 55 | `backend-llm-prompt#7` | medium | `covered_known_issue` | 已闭环：a70b88a（资源模板 provenance/upgrade） | preset 版本升级静默覆盖用户自定义模板 + 两段重复的裸 except: pass 吞升级错误；下一步：以基线 hash/dirty 标记选择性升级并记录 warning，之后毕业。 |
| 56 | `backend-llm-prompt#8` | low | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 死代码与半成品抽象：agent.py 全文件、strategy.py 半部、integrity 无生产调用、mojibake 死常量；下一步：删除死代码并用引用扫描确认无生产调用。 |
| 57 | `backend-llm-prompt#9` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 事实来源多份硬编码：provider 支持参数、默认 base_url、JSON 容错解析各有 2-4 份拷贝；下一步：把事实移入 registry，并让 provider/route 只消费同一表；补唯一来源断言。 |
| 58 | `backend-llm-prompt#10` | medium | `covered_known_issue` | 已闭环：f16b9f3（Prompt Studio output contract） | 双任务目录不同步 + Prompt Studio 输出契约靠中文案文本 find 匹配（脆弱设计）；下一步：统一结构化 output contract 标识后 known_issue 毕业，避免中文 heading find。 |
| 59 | `backend-llm-prompt#11` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 全链路同步阻塞 I/O + 三层 time.sleep，流式生成独占线程池线程（并发天花板）；下一步：并发压测/假时钟统计线程占用和总退避时间；迁异步或共享执行器。 |
| 60 | `backend-llm-prompt#12` | low | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | registry 模型契约过时且 pricing 全为空占位，capabilities/audit 存在无调用包装；下一步：若保留 pricing，填真实数据并测计费；否则删除 dead wrappers/placeholder。 |
| 61 | `backend-rag-memory#1` | critical | `cleanup_only` | 已闭环：e218f62（vector backend/facade 统一） | vector_build.py 与 vector_storage.py 约 900 行整体复制且两份副本同时生效，伴随循环导入与状态分裂；下一步：合并 build/storage 单一实现，再让 C3 毕业并保留 facade 回归。 |
| 62 | `backend-rag-memory#2` | critical | `covered_known_issue` | 已闭环：03f263d（fallback 写读同源） | chromadb 缺失时内存兜底写读分家：ingest 写入 storage 的 _INMEMORY_CHROMA、query 读 build 的，检索永远为空且无日志；下一步：共享 store/单实现后转 safety。 |
| 63 | `backend-rag-memory#3` | high | `pg_only` | 已闭环：a868f76 + PG fixture follow-ups（KB隔离） | pgvector 后端无 kb_id 维度：删除单个 KB 会清空整个项目的向量索引，多 KB 查询被静默折叠为 1 个；下一步：在真实 pgvector job 建两个 KB，删除一个后断言另一 KB chunks 保留并可分别查询。 |
| 64 | `backend-rag-memory#4` | high | `uncovered_testable` | 已闭环：07b6a23（ownership/reuse）；进程崩溃原子性不承诺 | 多 KB 重建对每个 KB 重复全量 embedding（N 倍 API 费用），且所有 KB 内容完全相同，多 KB 特性形同虚设；下一步：mock embedding，两个 KB 重建应只嵌入各自内容/总调用量不做 N 倍全量重复。 |
| 65 | `backend-rag-memory#5` | high | `covered_known_issue` | 已闭环：40fa93b（source order） | 正则字符类写错 r"[\\s,\|;]+"：vector_source_order 与 super_sort.source_order 字符串形式静默失效（已实测复现）；下一步：修正为 r"[\s,\|;]+" 后毕业，并保留字符串/列表两种输入。 |
| 66 | `backend-rag-memory#6` | high | `covered_known_issue` | 部分推进：614b502 仅有界 LIKE；FTS 真读路径未闭环 | 项目内搜索绕过自己维护的 FTS 索引做无 LIMIT 全表 LIKE 扫描，整套 FTS/search_rebuild 链路沦为只写不读；下一步：选择 FTS 读路径或有界 LIKE；修复后毕业并保留结果语义测试不锁 mode。 |
| 67 | `backend-rag-memory#7` | high | `pg_only` | 核心维度/CJK闭环：41c6b89/01c0f3e/0ca10cd；显式PG readiness与model identity另审 | pgvector 表硬编码 vector(1536) 与可配置 embedding 模型冲突（维度不符静默换后端）；FTS 用 simple 分词对中文语料基本无效；下一步：真实 pgvector 以非 1536 embedding 建/查；PG 中文语料对比 tokenizer 命中率。 |
| 68 | `backend-rag-memory#8` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 死代码与 schema 漂移：fractal_memory 等模块已删但测试+迁移残留（至少 8 个测试文件收集报错）、structured_memory 5 张表零读写、VectorRagProfile 后端零消费、worldbook 幽灵源；下一步：删除死模型/迁移残留或明确保留策略；避免给待删功能补 happy-path。 |
| 69 | `backend-rag-memory#9` | medium | `covered_known_issue` | 已闭环：c6bd69d（三源单一事实源） | _ALL_SOURCES 三处定义不一致（4源 vs 3源），overfilter relax 与默认过滤行为随导入源漂移；下一步：统一单一常量后 known_issue 毕业。 |
| 70 | `backend-rag-memory#10` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 查询路径 pgvector 失败静默降级无日志，全域 bare-except 吞错使排障只能靠猜；下一步：注入 PG 查询异常，断言 fallback 结果同时含 warning/log event，不含敏感信息。 |
| 71 | `backend-rag-memory#11` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 重建调度器三段式复制约 350 行：vector/search 调度同构，search 内部再自我复制两遍，_mark_vector_index_dirty 在 5 个路由文件各抄一份；下一步：抽统一 rebuild scheduler/mark_dirty 后复用现有事件与幂等测试。 |
| 72 | `deploy#1` | high | `covered_known_issue` | 已闭环原 OR 契约：eeb8255；4858/显式PG硬失败另审 | 默认Docker部署中向量RAG双后端全部静默失效（pgvector+Chroma均不可用），与README宣传能力不符；下一步：更换 pgvector 镜像或安装 Chroma 任一可靠后端后毕业，并更新 README。 |
| 73 | `deploy#2` | high | `covered_known_issue` | 已闭环主迁移覆盖：0c4cb20 + 2d25b00 验证 fail-closed | README引用的SQLite→PG迁移工具表清单严重过期，迁移静默丢弃约一半业务表；下一步：更新/替代迁移工具后毕业。 |
| 74 | `deploy#3` | high | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | run_quality_gate.py彻底损坏：引用不存在的../scripts/guards且硬编码Windows反斜杠路径；下一步：继续在 Linux CI 执行，禁止平台专属路径回归。 |
| 75 | `deploy#4` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | compose内置弱默认数据库口令回退，缺.env也能静默以ainovel/ainovel起库；下一步：解析 compose，要求生产部署无 ${VAR:-ainovel} 弱 fallback，缺 secret 时 fail-fast。 |
| 76 | `deploy#5` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | nginx add_header继承陷阱：所有HTML/静态资源响应丢失安全响应头；下一步：nginx -T 或容器 smoke，对 / 与静态资源断 CSP/XFO/nosniff 等头。 |
| 77 | `deploy#6` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 配置三源分裂：compose环境白名单缺失大量可调项，Docker运维无法配置向量/会话参数；下一步：解析 Settings 字段/显式 allowlist，与 compose env 做声明式差异检查。 |
| 78 | `deploy#7` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 本地开发路径与README不符：python start.py对新人必然失败；下一步：临时目录/最小 env 执行文档命令，至少启动到 health ready。 |
| 79 | `deploy#8` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | backend/.env.example预填弱凭据：admin/admin-pass与dev认证回退默认启用；下一步：对两个 .env.example 建敏感默认值/生产开关 lint。 |
| 80 | `deploy#9` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | entrypoint.sh为226行sh+三段Python heredoc的上帝入口，业务逻辑不可测试；下一步：抽 python -m app.bootstrap，entrypoint 仅编排并加容器 smoke。 |
| 81 | `deploy#10` | medium | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | 未入库目录anyrouter-solution/与项目无关且是密钥泄露高危位；下一步：保持 .gitignore/secret scan，若再次出现立即阻断。 |
| 82 | `deploy#11` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | scripts目录调试遗留污染生产镜像，verify脚本应为pytest测试；下一步：拆 runtime/dev scripts，并检查最终镜像不含 verify/debug 工具。 |
| 83 | `deploy#12` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 根.dockerignore是死配置且rq_worker无healthcheck；下一步：解析 compose/Docker build context，断 worker healthcheck 与实际生效 ignore 文件。 |
| 84 | `frontend-arch#1` | high | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 无服务端状态库，三套自制数据获取模式并存（手写不完整的 react-query）；下一步：选统一 server-state 层并迁移一条垂直链路，保持缓存/错误/失效契约。 |
| 85 | `frontend-arch#2` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | chapterStore 与 projectTaskStore 为同一模板复制粘贴，normalizeApiError 存在三份逐字拷贝；下一步：抽 store factory/toApiError 后用同一参数化 suite 跑两资源。 |
| 86 | `frontend-arch#3` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | usePromptsPageState 为 1768 行/43 个 useState 的上帝 hook，且反向依赖视图组件 props 类型；下一步：拆职责 hook 并把共享 types 移到中立模块。 |
| 87 | `frontend-arch#4` | high | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 类型层手抄后端 Pydantic schema 无 codegen，drift 已实际发生；双 barrel 重复导出；下一步：导出 OpenAPI/codegen，并在 CI 断生成产物无 diff。 |
| 88 | `frontend-arch#5` | high | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | useWizardProgress 在 9 个页面各自并发 6 个 GET 且无缓存，进度标记寄存 localStorage；下一步：RTL/renderHook 同时挂多个消费者，断言同项目只发一组请求且 server summary 为唯一真源。 |
| 89 | `frontend-arch#6` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | wizard.ts 内 110 行整段复制：computeWizardProgress 与 computeWizardProgressFromSummary；下一步：收敛为一个纯函数并用相同输入参数化测试两旧入口。 |
| 90 | `frontend-arch#7` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | API 端点字符串散落 25+ 页面文件，服务层覆盖率低且 URL 编码不一致；下一步：集中 endpoints builder，并测试 path/query 编码与唯一性。 |
| 91 | `frontend-arch#8` | medium | `covered_known_issue` | 大幅推进但未闭环：多个页面与query hook已收敛；Entries/Settings仍缺 | 错误处理约定三分裂，useProjectData 不向调用方暴露 error 状态；下一步：暴露 error/reset/retry 后毕业。 |
| 92 | `frontend-arch#9` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 任务实时事件系统被删除后遗留 no-op 存根与死抽象，实时性静默降级为 8 秒轮询；下一步：删除存根或恢复事件源；假时钟断言更新延迟/轮询退避。 |
| 93 | `frontend-arch#10` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 错误协议处理块在 apiClient 三函数与 sseClient 中重复 4 份，401 依赖字符串 window 事件总线；下一步：抽共享 decodeApiError + typed auth channel，复用当前测试矩阵。 |
| 94 | `frontend-arch#11` | medium | `covered_known_issue` | 已闭环：2408cb2（Admin 双层守卫） | /admin/users 无路由级权限守卫，admin 判定埋在页面内部；下一步：在路由层增加 admin guard 并同步过滤导航后，两条一起毕业。 |
| 95 | `frontend-arch#12` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | AppShell 自制 keep-alive 缓存脱离路由树的 ReactNode，复杂度扩散到所有页面；下一步：MemoryRouter 多次切换项目/页面，断言无陈旧 ReactNode、订阅正确释放且缓存有界。 |
| 96 | `frontend-pages#1` | critical（建议降 high） | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | usePromptsPageState.ts 1768 行上帝 Hook：36+ useState 横跨 7 个职责域，状态管理失控；下一步：按职责拆 hook，新增每个公开 hook 的状态转换测试。 |
| 97 | `frontend-pages#2` | high | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 三个 prompt 页面功能重叠，PromptTemplatesPage(1060行) 无任何导航入口为准死代码，/prompts 路由名不副实；下一步：确认兼容需求后删死页面/旧 API，并同步删测试。 |
| 98 | `frontend-pages#3` | high | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 全项目零 React.memo + 巨型 props 包每渲染重建 → 输入类交互整页重渲染；下一步：React Profiler 或 Playwright 输入场景记录 commit 数/响应时间基线。 |
| 99 | `frontend-pages#4` | high | `partial` | 大幅推进但未闭环：c967ead/ff3d3fe 清盲转型；toast/presentation 样板仍多 | 错误处理样板 82 处复制粘贴 + 77 处 as ApiError 不安全盲转型；下一步：引入 toApiError/toastApiError + ESLint 禁断言，并将该 known_issue 毕业。 |
| 100 | `frontend-pages#5` | high | `covered_known_issue` | 已闭环：91a1363（merge-latest save queue） | 手写『保存中排队』并发控制（savingRef+queuedSaveRef）在 5 个文件重复实现；下一步：修 merge-latest 并迁移 5 处复制实现到同一 hook 后毕业。 |
| 101 | `frontend-pages#6` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | SSE 流式重连循环在 outline 两个 hook 中逐字重复；下一步：收敛 connectWithRetry，假时钟参数化 retry/abort/early-close。 |
| 102 | `frontend-pages#7` | medium | `covered_known_issue` | 已闭环：c686dd0（import stalled UI） | ImportPage 轮询停滞检测失效：Date.now() 差值被 useMemo 冻结；下一步：引入 tick state/interval 后毕业。 |
| 103 | `frontend-pages#8` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | entries 两处『翻页到底』全量加载 + 客户端过滤，无虚拟化；下一步：mock 多页大数据，断请求页数/增量渲染；必要时浏览器性能基线。 |
| 104 | `frontend-pages#9` | medium | `cleanup_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | prompts 组件层重复定义：PROVIDER_OPTIONS、TaskModuleView、provider 能力判定三处复制；下一步：抽单一 provider registry/component，保留现有 provider 分支测试。 |
| 105 | `frontend-pages#10` | medium | `covered_known_issue` | 已闭环：ddca1f2（创建后列表刷新） | AdminUsersPage 移动卡片与桌面表格双份复制同一行为按钮，创建用户后列表可能不刷新；下一步：create 成功后 invalidate/refetch 或本地插入，再毕业；双布局行为可后续组件化。 |
| 106 | `frontend-pages#11` | medium | `integration_only` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 智能解析应用角色/条目为串行 N+1 请求，无批量与事务性；下一步：mock API 批次应用，断调用数 O(1)、并行上限与失败时不出现半应用。 |
| 107 | `frontend-pages#12` | medium | `uncovered_testable` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | Hook 返回 no-op 回调由页面层再打补丁覆盖，按钮行为分裂在两层；下一步：渲染实际页面按钮，断每个 action 触发唯一非 no-op handler；随后移除双层补丁。 |
| 108 | `tests#1` | critical | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | 测试套件整体红灯且无任何 CI 强制执行；下一步：持续监控 CI 实跑；coverage 基线只升不降。 |
| 109 | `tests#2` | critical | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | lite 分支删功能未删测试：21+ 测试文件 import 已删除模块；下一步：以后删功能必须同步删测试并由 CI collection 阻断。 |
| 110 | `tests#3` | critical | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | 测试引用整仓已删除的基础设施（scripts/guards、run_gate、Playwright e2e）；下一步：若恢复 e2e，必须同时恢复可执行基础设施和 CI job。 |
| 111 | `tests#4` | high | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | 17 个断言失败=真实行为漂移无人 triage（含幂等契约）；下一步：每次修 bug 将对应 known_issue 转 safety，禁止以 skip/弱断言消红。 |
| 112 | `tests#5` | refuted（余留 medium 直接测试缺口） | `refuted` | 基线已反驳；阶段一未重新实现，保留终审裁决并复核证据 | 核心 service 大面积零测试：向量子系统三大文件共 3188 行无直接单测；下一步：按真实缺口补 llm_key_resolver/prompt render 等，不重复建立向量零覆盖问题。 |
| 113 | `tests#6` | high | `pg_only` | 阶段一已闭环原 gap：c6f7d1b 建立真实 PostgreSQL+pgvector hard job，6e5ca23 修绿并持续覆盖迁移/并发/索引/vector；覆盖范围仍应扩展 | 测试库与生产库不同构：全量 sqlite 内存库，pgvector/PG 行为默认零验证；下一步：CI 增 Postgres+pgvector service job，迁移/并发/索引/vector 测试常规化。 |
| 114 | `tests#7` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 前端测试形同虚设：node 环境纯函数+文案断言，69 个 TSX 组件零交互测试；下一步：继续覆盖写作任务面板/路由守卫/导入主链路，并提高 coverage 基线。 |
| 115 | `tests#8` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 60+ 份复制粘贴测试脚手架：_make_test_app 定义 29 次，无 conftest/共享模块；下一步：按触碰迁移原则逐步改用 support，禁止新增第 30 份 _make_test_app。 |
| 116 | `tests#9` | medium | `resolved` | 基线已 resolved；阶段一未重复实施，后续只做回归复核 | 测试运行体系原始：无 pytest/coverage/文档，依赖未固化的环境；下一步：持续在 CI 中验证 lock 安装和 thresholds 稳定，基线只升不降。 |
| 117 | `tests#10` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 测试深挖路由私有函数，锁死实现细节；下一步：新增需求改测 service/public route；重构时将高价值契约迁至 public surface。 |
| 118 | `tests#11` | medium | `partial` | 阶段一未闭环/未逐项重审；沿用 baseline gap，不得视为完成 | 后端测试断言用户可见中文文案，跨层脆耦合；下一步：区分 prompt 输出合同与 UI 文案：前者保留，后者改 code/结构/可访问角色。 |
<!-- CATALOG_118_END -->

---

## 12. 最后交接结论

第一阶段最大的价值不是“改了很多文件”，而是把项目从以下危险状态拉回了更可靠的基线：

- 章节 replace 可能永久丢数据 → 事务原子。
- 多 KB pgvector 可能串库/清错数据 → project+KB 隔离。
- worker 重投可能双跑 → 独占 claim。
- 迁移验证失败仍 exit 0 → fail-closed。
- 禁用用户旧 cookie 仍可访问 → session epoch/version 撤销。
- generation 隐式创建 prompt → 显式同步、缺配置 fail-closed。
- RAG 多 KB 重复 embedding/内容同质 → ownership-aware rebuild。
- 中文/维度契约薄弱 → 真实 PG 维度/CJK/trigram 测试。
- 前端把错误伪装为空数据 → initial/stale/empty 分层和内联 retry。
- query hook 自带 toast → presentation-free state contract。

但项目仍处于“长期整改进行中”，不是完成态。下一位接手者最应避免两件事：

1. 因为当前 CI 全绿就宣布 118 项完成；
2. 因为某个 catalog 有多个 commit 就忽略其仍未覆盖的子路径。

应继续沿用第一阶段已经建立的证据闭环、真实环境验证、独立 review 和原子提交纪律，先完成 Entries/Settings，再解决 rate-limit 私有依赖和 pgvector 硬失败语义，最后重新生成一份真正逐行更新后的 118 项 authoritative reconciliation。
